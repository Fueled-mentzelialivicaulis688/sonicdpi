[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update && apt-get install --assume-yes libnetfilter-queue-dev:$CROSS_DEB_ARCH libgtk-3-dev:$CROSS_DEB_ARCH libxdo-dev:$CROSS_DEB_ARCH libayatana-appindicator3-dev:$CROSS_DEB_ARCH pkg-config",
]
